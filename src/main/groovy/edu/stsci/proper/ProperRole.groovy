package edu.stsci.proper

/**
 * NOTE: In the real application, this is a GORM domain class with many possible roles.
 * For this exercise, it has been simplified to an enum with a single value.
 */
enum ProperRole {
    INVESTIGATOR
}
